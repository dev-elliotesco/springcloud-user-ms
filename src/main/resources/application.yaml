spring:
  application:
    name: user-ms

  datasource:
    url: jdbc:mysql://localhost:3308/user_db
    username: root
    password: Netec123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

server:
  port: 8081